# Renommer en .env (à la racine du dossier docker/) et remplir avec vos vraies valeurs IAM.
# Le fichier .env est ignoré par Git — ne le commitez pas.

AWS_S3_BUCKET=waitless-logos
AWS_S3_REGION=eu-north-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
